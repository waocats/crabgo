[workspace]
exclude = ["dependency"]

[package]
name = "crabgo-list-test-fixture"
version = "0.0.0"

[dependencies]
crabgo-list-test-fixture-dependency = { version = "0.0.0", path = "dependency", optional = true, default-features = false, features = ["one", "two"], registry = "alternative" }

[dev-dependencies]
crabgo-list-test-fixture-dependency = { path = "dependency" }
